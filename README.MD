Project Structure
/project-root
│── index.php            # Paycheck Calculator page
│── guestbook.php        # Guestbook form & list of entries
│── nav.php              # Navigation bar shared by all pages
│── config.php # Handles paycheck calculations (if separate)
│── paycheckcalculator.php       # Processes guestbook form submissions
│── paycheck result.php           # Optional custom CSS
└── about.php        # SQL table structure